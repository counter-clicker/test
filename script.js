const button = document.getElementById('cta');
const message = document.getElementById('message');

button.addEventListener('click', () => {
  message.textContent = 'Top! Je website werkt en is klaar voor jouw volgende stap.';
});
